/**
 * Represents a rendered dot Element.
 */
export interface DotElement<N = GraphNode> {
    id: string;
    nodes: N[];
    text: string;
}
/**
 * An input graph node.
 */
export interface GraphNode {
    children?: GraphNode[];
    /**
     * Desired output .dot graph node label
     */
    label: string;
    /**
     * Desired dot output element attributes.
     */
    attrs?: {
        [name: string]: any;
    };
    /**
     * An unique graph node identifier. If none is given, one will be set.
     */
    id?: string;
}
/**
 * Will render given graph represented by [[node]] as a .dot string.
 */
export declare function graphToDot({ node, name, cluster, rankdir }: {
    node: GraphNode;
    name?: string;
    cluster?: boolean;
    rankdir?: 'TB' | 'LR' | 'BT' | 'RL';
}): {
    dot: string;
    elements: DotElement<GraphNode>[];
};
export declare function getNodeId(n: GraphNode): string;
