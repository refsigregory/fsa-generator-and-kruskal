import { fabric } from 'fabric';
import { IImageOptions, Image } from 'fabric/fabric-impl';
import { Format } from './types';
export declare const fabricImageFromUrl: (arg1: string, arg2: fabric.IImageOptions) => Promise<fabric.Image>;
export declare function fabricImageFromData(data: string, format: Format, options?: IImageOptions): Promise<Image>;
