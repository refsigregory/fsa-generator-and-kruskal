/// <reference types="node" />
import { SVG2PNGOptions } from './types';
interface EncodingValueMap {
    dataURL: string;
    base64: string;
    raw: string;
    buffer: Buffer;
}
/**
 * Rasterize given svg to PNG or JPEG bitmap image.
 */
export declare function svg2png<E extends keyof EncodingValueMap, T = EncodingValueMap[E]>(options: SVG2PNGOptions<E>): Promise<T>;
export {};
